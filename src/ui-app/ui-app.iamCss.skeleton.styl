/* Size scheme */
[{bApp} *= 'leftNav'] {
    display: block;
    box-sizing: border-box;
    margin-left: --nav-w;
    padding: 2em;
    min-height: 12em;
}

[{eNav} *= 'leftNav'] {
    position: fixed;
    left: 0;
    top: 0;
    width: --nav-w;
}
